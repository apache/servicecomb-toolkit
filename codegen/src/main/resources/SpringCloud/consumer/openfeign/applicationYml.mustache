spring:
  application:
    name: {{consumerServiceId}}
server:
  port: 0
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/